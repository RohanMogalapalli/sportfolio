const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;
if(!reduce){const observer=new IntersectionObserver(items=>items.forEach(({isIntersecting,target})=>{if(isIntersecting){target.classList.add('visible');observer.unobserve(target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(x=>observer.observe(x));const orb=document.querySelector('.orbital'),hero=document.querySelector('.hero');hero.addEventListener('pointermove',e=>{orb.style.transform=`translate(${(e.clientX/innerWidth-.5)*20}px,${(e.clientY/innerHeight-.5)*16}px) rotate(${(e.clientX/innerWidth-.5)*5}deg)`});hero.addEventListener('pointerleave',()=>orb.style.transform='');document.querySelectorAll('.visual').forEach(v=>{v.addEventListener('pointermove',e=>{const r=v.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;v.style.transform=`perspective(900px) rotateX(${-y*2}deg) rotateY(${x*2}deg)`});v.addEventListener('pointerleave',()=>v.style.transform='')})}
