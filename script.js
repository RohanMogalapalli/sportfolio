const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;
if(!reduce){const o=new IntersectionObserver(e=>e.forEach(({isIntersecting: i,target: t})=>{if(i){t.classList.add('visible');o.unobserve(t)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(e=>o.observe(e));const orb=document.querySelector('.orbital'),hero=document.querySelector('.hero');if(orb&&hero){hero.addEventListener('pointermove',e=>{orb.style.transform=`translate(${(e.clientX/innerWidth-.5)*20}px,${(e.clientY/innerHeight-.5)*16}px) rotate(${(e.clientX/innerWidth-.5)*5}deg)`},{passive:true});hero.addEventListener('pointerleave',()=>orb.style.transform='',{passive:true})}document.querySelectorAll('.visual').forEach(v=>{v.addEventListener('pointermove',e=>{const r=v.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;v.style.transform=`perspective(900px) rotateX(${-y*2}deg) rotateY(${x*2}deg)`},{passive:true});v.addEventListener('pointerleave',()=>v.style.transform='',{passive:true})})}
